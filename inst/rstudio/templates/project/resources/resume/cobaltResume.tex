
\documentclass{cobaltResume}
\usepackage{catchfilebetweentags} % For extracting code between tags
\usepackage{tabularx}

% Check whether YAML variables are defined and then create a LaTeX command that prints them
$if(githubName)$
\githubName{$githubName$}
$endif$

$if(githubLink)$
\githubLink{$githubLink$}
$endif$

$if(date)$
\date{$date$}
$endif$

$if(personalWebsite)$
\personalWebsite{$personalWebsite$}
$endif$

$if(linkedinName)$
\linkedinName{$linkedinName$}
$endif$

$if(linkedinLink)$
\linkedinLink{$linkedinLink$}
$endif$

$if(email)$
\email{$email$}
$endif$

$if(firstName)$
\firstName{$firstName$}
$endif$

$if(lastName)$
\lastName{$lastName$}
$endif$

$if(position)$
\position{$position$}
$endif$

$if(skills)$
\skills{$skills$}
$endif$

$if(citizenship)$
\citizenship{$citizenship$}
$endif$

$if(shortDescription)$
\shortDescription{$shortDescription$}
$endif$

%load additional latex packages needed within document
$for(header-includes)$
	$header-includes$
$endfor$

% \renewcommand{\familydefault}{qhv}

% set default font to Helvetica
\usepackage[scaled]{helvet}
\renewcommand\familydefault{\sfdefault}
%\setlength{\headsep}{3cm}
\usetikzlibrary{fit}


\renewcommand{\rmdefault}{\sfdefault}

\begin{document}

  \thispagestyle{cobaltHeader}
  \pagestyle{cobaltFooter}
  \vspace*{3cm}

\columnratio{0.23}
 \begin{paracol}{2}
    \setlength{\columnseprule}{0.75pt}
     \setlength{\columnsep}{2em}
      \vspace*{-0.49cm}

    \raggedright
    \par\setstretch{1.25}\noindent
    %repeat content in left column on each page
    \newcounter{x}
     \IfFileExists{metadata.tex}{
      \forloop{x}{0}{\value{x} < \ztotpages}{
      % Execute metadata contents
        \ExecuteMetaData[metadata.tex]{education}
        \vspace{0.5cm}
        \ExecuteMetaData[metadata.tex]{selected-white-papers}
        \pagebreak
      }
     }

    \switchcolumn

    \raggedright

      \fontsize{9}{9}\selectfont
      \IfFileExists{metadata.tex}{
        \ExecuteMetaData[metadata.tex]{profile}
        \ExecuteMetaData[metadata.tex]{skills}
        \ExecuteMetaData[metadata.tex]{employment-experience}
        \ExecuteMetaData[metadata.tex]{data-science-experience}
      }

\end{paracol}





  $body$


\end{document}


