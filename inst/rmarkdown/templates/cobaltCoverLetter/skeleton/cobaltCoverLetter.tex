
\documentclass{cobaltCoverLetter}

% Check whether YAML variables are defined and then create a LaTeX command that prints them
$if(githubName)$
\githubName{$githubName$}
$endif$

$if(githubLink)$
\githubLink{$githubLink$}
$endif$

$if(date)$
\date{$date$}
$endif$

$if(personalWebsite)$
\personalWebsite{$personalWebsite$}
$endif$

$if(linkedinName)$
\linkedinName{$linkedinName$}
$endif$

$if(linkedinLink)$
\linkedinLink{$linkedinLink$}
$endif$

$if(email)$
\email{$email$}
$endif$

$if(firstName)$
\firstName{$firstName$}
$endif$

$if(lastName)$
\lastName{$lastName$}
$endif$

$if(position)$
\position{$position$}
$endif$

$if(skills)$
\skills{$skills$}
$endif$

%company & recruiter info
$if(recruiterName)$
\recruiterName{$recruiterName$}
$endif$

$if(organization)$
\organization{$organization$}
$endif$

$if(organizationStreet)$
\organizationStreet{$organizationStreet$}
$endif$

$if(organizationCityPostal)$
\organizationCityPostal{$organizationCityPostal$}
$endif$

$if(citizenship)$
\citizenship{$citizenship$}
$endif$

%load additional latex packages needed within document
$for(header-includes)$
	$header-includes$
$endfor$

%\setlength{\headsep}{3cm}
\usetikzlibrary{fit}
\usepackage{fontsize}

\renewcommand{\rmdefault}{phv}
\setlength{\parskip}{.375cm}


\begin{document}
  \changefontsize[9pt]{9pt}
  \thispagestyle{cobaltHeader}
   \vspace*{2cm}
  \setstretch{1.1}

  \noindent Dear $recruiterFormalName$


  $body$

  \noindent
  $emailClosing$,
  \vspace*{0.75cm}

  \noindent
  \textbf{$firstName$ $lastName$}




\end{document}


